# react-exp2-basic-components
Create simple React components such as **Header**, **Footer**, and **Content**, and render them inside `App.jsx`.   This experiment demonstrates React’s component-based architecture.
